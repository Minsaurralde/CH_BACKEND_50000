const cluster = "codercluster";
const user = "CoderUser";
const password = "Mi34152384";

const uri = `mongodb+srv://${user}:${password}@${cluster}.1vtqohs.mongodb.net/?retryWrites=true&w=majority`;

export default uri;
